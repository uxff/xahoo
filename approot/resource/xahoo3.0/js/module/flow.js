//瀑布流
	Tab.prototype._scroll = function(){
		
		$(window).on('scroll',function(){
		    if($(window).scrollTop()>=$(document).height()-$(window).height()){
		       
		    }
		
		})
		return this;
	}